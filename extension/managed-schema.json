{
  "type": "object",
  "properties": {
    "dashboardUrl": {
      "title": "Dashboard URL",
      "description": "Base URL of the wellbeing-team dashboard (MDM-forced).",
      "type": "string"
    },
    "campusCode": {
      "title": "Campus code",
      "description": "College identifier bound via Chrome Enterprise policy.",
      "type": "string"
    },
    "collegePolicyUrl": {
      "title": "College acceptable-use policy URL",
      "type": "string"
    },
    "optOutAllowed": {
      "title": "Allow student opt-out of telemetry",
      "type": "boolean"
    },
    "dpEpsilon": {
      "title": "Local differential-privacy epsilon",
      "description": "Randomised-response epsilon applied to the category label before egress (Erlingsson et al., RAPPOR 2014). Higher = less noise, more accurate aggregates. 0 disables the mechanism. Default 1.0.",
      "type": "number"
    }
  }
}
